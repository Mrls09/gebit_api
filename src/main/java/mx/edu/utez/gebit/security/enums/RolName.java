package mx.edu.utez.gebit.security.enums;

public enum RolName {
    //El aplicativo tiene dos roles:
    // admin(Tiene todos los permisos CRUD) y user(ESTUDIANTES ) y role
    ROLE_ADMIN, ROLE_USER , ROLE_TEACHER
}
